{
	"name": "shell-man",
	"displayName": "shell-man",
	"description": "manager shell scripts for VS Code",
	"version": "0.0.1",
	"publisher": "panda20160914",
	"repository": "https://github.com/angrypandahu/shell-man",
	"engines": {
		"vscode": "^1.74.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onView:shellManProjects"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "shellManProjects.refresh",
				"title": "刷新项目列表",
				"icon": "$(refresh)"
			},
			{
				"command": "shellManProjects.showLogs",
				"title": "显示日志",
				"icon": "$(output)"
			}
		],
		"viewsContainers": {
			"activitybar": [
				{
					"id": "shellManContainer",
					"title": "Shell 命令管理器",
					"icon": "$(terminal-bash)"
				}
			]
		},
		"views": {
			"shellManContainer": [
				{
					"id": "shellManProjects",
					"name": "Shell 命令",
					"icon": "$(terminal-bash)",
					"contextualTitle": "Shell 命令"
				}
			]
		},
		"menus": {
			"view/title": [
				{
					"command": "shellManProjects.refresh",
					"when": "view == shellManProjects",
					"group": "navigation"
				},
				{
					"command": "shellManProjects.showLogs",
					"when": "view == shellManProjects",
					"group": "navigation"
				}
			]
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"lint": "eslint",
		"watch": "tsc -watch -p ./"
	},
	"devDependencies": {
		"@eslint/js": "^9.13.0",
		"@stylistic/eslint-plugin": "^2.9.0",
		"@types/node": "^20",
		"@types/vscode": "^1.73.0",
		"eslint": "^9.13.0",
		"typescript": "^5.8.2",
		"typescript-eslint": "^8.26.0"
	}
}